node_modules/
dist/
.vite/
shots/
*.local
.DS_Store
Thumbs.db
.env
.env.*
# Cloudflare Pages local secrets — the Azure key for `wrangler pages dev`.
# NEVER committed: the key lives ONLY in Cloudflare's encrypted env at runtime.
.dev.vars
screenshots/

.claude/worktrees/
