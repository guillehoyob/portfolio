/**
 * WHITE NOON — Weather (SKY-5..11) — STUB (Fase 0)
 * W1-cielo-clima replaces this file entirely: Capa 0 sintética determinista +
 * Capa 1 Open-Meteo (ONE GET to api.open-meteo.com — the single sanctioned
 * third-party, CONTRACTS §f.4), tz-table coords, WMO→data-wx mapping, and the
 * sky phenomena (clouds/rain/snow/storm/sprite/fog/iris). Flag-gated no-op here
 * so the boot line, flag and chunk exist from Fase 0 on.
 */
export function initWeather() { /* W1 */ }
